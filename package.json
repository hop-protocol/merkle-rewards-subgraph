{
  "name": "merkle-rewards",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ hop-protocol/merkle-rewards",
    "create-local": "graph create --node http://localhost:8020/ hop-protocol/merkle-rewards",
    "remove-local": "graph remove --node http://localhost:8020/ hop-protocol/merkle-rewards",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 hop-protocol/merkle-rewards"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.25.3",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
